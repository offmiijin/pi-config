# pi-custom-theme

Extensões visuais para o TUI do pi.

Atualmente registra a barra de status com informações do modelo, tokens, branch e estado do workspace isolado. Também integra estatísticas emitidas pelo `pi-memory`.

A extensão é carregada pelo entrypoint `index.ts`; não possui comando próprio.
