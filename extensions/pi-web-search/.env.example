# SearXNG Docker — opcional (alternativas: Tavily, Exa ou Serper)
# Generate a random secret: openssl rand -hex 32
SEARXNG_KEY=changeme_generate_a_random_64_char_hex_string

# Optional: override default SearXNG URL
# SEARXNG_URL=http://localhost:4000
